<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Profile</title>
    <link rel="stylesheet" href="/public/css/styles.css">
</head>

<body>
    <h2 class="h1">Edit Profile</h2>
    <form action="/editprofile" method="POST">
        <div>
            <label for="firstNameInput">First Name:</label>
            <input type="text" name="firstNameInput" required value="{{firstName}}" class="input-field">
           
        </div>

        <div>
            <label for="lastNameInput">Last Name:</label>
            <input type="text" name="lastNameInput" required value="{{lastName}}" class="input-field">
            
        </div>

        <div>
            <label for="usernameInput">Username:</label>
            <input type="text" name="usernameInput" required value="{{userName}}" class="input-field">
           
        </div>

        <button type="submit" class="button">Save Changes</button>
    </form>
    <div id="error-message" class="error"></div>
      <form id="login-form"> </form>
<form id="registration-form"> </form>
    <div class="back-link">
    <p><a href="/" class="link link-size">Home</a></p>
    </div>
    <script src="/public/js/client_side_validation.js"></script>
</body>

</html>